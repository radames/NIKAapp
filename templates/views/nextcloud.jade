extends ../layouts/default

block content
	.container: .jumbotron
		h1 Nextcloud
		hr
		if user && user.canAccessKeystone
			.alert.alert-info
				strong Download der Nextcloud-Inhalte hier!
				|<br>
				|(Upload und Änderung der Daten auf Nextcloud ist nur über den individuellen Nextcloud-Account möglich)
			p
				a(href=" , target="_blank").btn.btn-lg.btn-primary Open the Open Nextcloud Data
		hr
