extends ../layouts/default

block content
	.container: .jumbotron
		h1 Welcome
		hr
		if user && user.canAccessKeystone
			p
				a(href='/admin').btn.btn-lg.btn-primary Open the Admin UI
		else
			p
				a(href='/admin/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Sign in
				|  to use the Admin UI.
		hr
